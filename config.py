# Filepath to check if prompt_Root = True
root = './images'

# Program asks for path of images (True), or pre-set root (above) (False)
promptRoot = True

# Program says it's done when done (True), or instant close (False)
promptStop = True

# Program prints everything it does
debug = True

# Program writes all colors found (HEX only) (True), or don't ... (False)
writeColorsToFile = True
